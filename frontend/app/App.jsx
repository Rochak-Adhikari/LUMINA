// frontend/app/App.jsx — root of the new modular frontend (parallel scaffold)
import React from 'react';
import { AppShell } from '../layout/AppShell';

export default function App() {
  return <AppShell />;
}
